name: .NET

on: [push]
  

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: 8.0.x
    - name: Run on update folder
      run: cd $GITHUB_WORKSPACE/update
    - name: Restore tools
      run: dotnet tool restore
    - name: Test
      run: dotnet run bundle
